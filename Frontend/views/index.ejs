<%- include('partials/header') %>

<section class="overflow-x-hidden">
    <!-- slider -->
    <%- include('slider') %>
    <% productsByCategory.forEach(({ category, products }, index) => { %>
        <h1 class="text-2xl font-bold text-center my-4">
            <%= category %>
        </h1>
        <%- include('./main/allSwipers/fiveRsNamkeen', { productData: products }) %>
        <% if ((index + 1) % 3 === 0) { %>
            <%- include('./main/template') %>
        <% } %>
    <% }) %>
</section>

<%- include('partials/footer') %>
